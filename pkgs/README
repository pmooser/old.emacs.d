The subtree command doesn't seem to embed the origin of the subtree anywhere. I'll keep some notes here.

# ac-nrepl 

subtree add --prefix pkgs/ac-nrepl https://github.com/clojure-emacs/ac-nrepl.git master --squash

# cl-lib and pkg-info added manually (no subtree)

# clojure-mode 

subtree add --prefix pkgs/clojure-mode https://github.com/clojure-emacs/clojure-mode.git master --squash

git subtree pull --prefix pkgs/clojure-mode https://github.com/clojure-emacs/clojure-mode.git master --squash


# adding nrepl.el
git subtree add --prefix pkgs/nrepl https://github.com/clojure-emacs/nrepl.el.git master --squash

# dash
git subtree add --prefix pkgs/dash https://github.com/magnars/dash.el.git master --squash

# yasnippet

git subtree add --prefix pkgs/yasnippet https://github.com/capitaomorte/yasnippet.git master --squash


